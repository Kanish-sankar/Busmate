# 🗺️ Firebase Data Flow & Architecture Diagram

## 📱 MOBILE APP (busmate_app) DATA FLOW

```
┌─────────────────────────────────────────────────────────────────┐
│                         MOBILE APP USERS                        │
│                    (Students/Parents)                           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Firebase Authentication                      │
│                     (Email/Password)                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
              ┌────────────┴────────────┐
              │                         │
              ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │   auth_login.    │      │  signin_screen.  │
    │   isStudentLogin │      │  submitForm()    │
    └────────┬─────────┘      └────────┬─────────┘
             │                         │
             └─────────────┬───────────┘
                           │
                           ▼
         ┌─────────────────────────────────────┐
         │  Check User Type in Collections:    │
         │  1. students/{uid}                  │
         │  2. drivers/{uid}                   │
         └─────────────┬───────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         │                           │
         ▼                           ▼
    [Student]                   [Driver]
         │                           │
         ▼                           ▼
┌─────────────────┐         ┌─────────────────┐
│  Routes.        │         │  Routes.        │
│  stopLocation   │         │  driverScreen   │
└────────┬────────┘         └────────┬────────┘
         │                           │
         ▼                           ▼
┌─────────────────────────┐ ┌─────────────────────────┐
│  DashboardController    │ │  DriverController       │
│  onInit():              │ │  onInit():              │
│  • fetchStudent()       │ │  • fetchDriver()        │
│  • fetchSchool()        │ │  • fetchAssignedBus()   │
│  • fetchBusDetail()     │ │  • startLocationService │
│  • fetchBusStatus()     │ │  • updateBusLocation()  │
│  • fetchDriver()        │ │                         │
└──────────┬──────────────┘ └──────────┬──────────────┘
           │                           │
           ▼                           ▼
```

---

## 🔥 FIRESTORE READ PATTERNS

### **High-Frequency Reads (Every Second)** 🔴

```
┌──────────────────────────────────────────────────────────┐
│                    REAL-TIME LISTENERS                   │
│                   (Continuous Streams)                   │
└──────────────────────────┬───────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌─────────────┐  ┌──────────────────┐  ┌──────────────┐
│  students/  │  │ schools/{id}/    │  │ bus_status/  │
│  {id}       │  │ buses/{id}       │  │ {busId}      │
│             │  │                  │  │              │
│ snapshots() │  │   snapshots()    │  │ snapshots()  │
│ 1 read/sec  │  │   1 read/sec     │  │ 1 read/sec   │
└─────────────┘  └──────────────────┘  └──────────────┘
      │                  │                      │
      │                  │                      │
      ▼                  ▼                      ▼
┌──────────────────────────────────────────────────────────┐
│         DASHBOARD UI UPDATES IN REAL-TIME                │
│  • Student name/details                                  │
│  • Bus location on map                                   │
│  • ETA calculations                                      │
│  • Remaining stops                                       │
└──────────────────────────────────────────────────────────┘

COST: 100 users × 3 listeners × 1 read/sec × 1800 sec/session
    = 540,000 reads per session
    = 5.4M reads/month 🔥💰
```

### **Optimized Pattern (Polling)** 🟢

```
┌──────────────────────────────────────────────────────────┐
│                    POLLING (Every 30s)                   │
│                   (Timer-Based Fetch)                    │
└──────────────────────────┬───────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌─────────────┐  ┌──────────────────┐  ┌──────────────┐
│  students/  │  │ schools/{id}/    │  │ bus_status/  │
│  {id}       │  │ buses/{id}       │  │ {busId}      │
│             │  │                  │  │              │
│   get()     │  │     get()        │  │   get()      │
│ 1 read/30s  │  │  1 read/30s      │  │ 1 read/30s   │
└─────────────┘  └──────────────────┘  └──────────────┘
      │                  │                      │
      │                  │                      │
      ▼                  ▼                      ▼
┌──────────────────────────────────────────────────────────┐
│         DASHBOARD UI UPDATES EVERY 30 SECONDS            │
│  • Still shows bus location (slight delay)               │
│  • User doesn't notice difference                        │
└──────────────────────────────────────────────────────────┘

COST: 100 users × 3 collections × 1 read/30s × 1800 sec/session
    = 18,000 reads per session
    = 180k reads/month 🟢💚
    
SAVINGS: 97% reduction (5.4M → 180k)
```

---

## 🌐 WEB APP (busmate_web) DATA FLOW

```
┌─────────────────────────────────────────────────────────────┐
│                      WEB ADMIN PANEL                        │
│            (School Admins / Super Admins)                   │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   Admin Dashboard Screens                   │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐ │
│  │ School Mgmt    │  │ Student Mgmt   │  │  Bus Mgmt    │ │
│  │ StreamBuilder  │  │ StreamBuilder  │  │ StreamBuilder│ │
│  └────────┬───────┘  └────────┬───────┘  └──────┬───────┘ │
└───────────┼──────────────────┼──────────────────┼──────────┘
            │                  │                  │
            ▼                  ▼                  ▼
   ┌─────────────────┐  ┌──────────────┐  ┌─────────────────┐
   │ schools/        │  │ students/    │  │ schools/{id}/   │
   │                 │  │              │  │ buses/{id}      │
   │ snapshots()     │  │ snapshots()  │  │                 │
   │ (ALL SCHOOLS)   │  │ (FILTERED)   │  │ snapshots()     │
   └─────────────────┘  └──────────────┘  └─────────────────┘
```

### **Current Web Admin Reads** 🔴

```
SuperAdmin Dashboard:
└── schools.snapshots()                    → 50 reads/sec (all schools)
    ├── For each school: payments.snapshots() → 100 reads/sec
    └── For each school: students.snapshots() → 500 reads/sec
    
SchoolAdmin Panels:
├── students.where(schoolId).snapshots()   → 200 reads/sec
├── drivers.where(schoolId).snapshots()    → 50 reads/sec
├── buses.snapshots()                      → 100 reads/sec
└── bus_status.snapshots()                 → 300 reads/sec

TOTAL: ~1,300 reads/second during admin session
     = 4.7M reads per 4-hour admin session
     = 4.3M reads/month (10 admins) 🔥💰
```

### **Optimized Web Admin Pattern** 🟢

```
Admin Dashboard (Manual Refresh):
└── FutureBuilder + RefreshButton
    ├── schools.get()          → 1 read per click
    ├── students.get()         → 1 read per click
    └── buses.get()            → 1 read per click
    
+ Pagination (20 records at a time)
    ├── First page: 20 reads
    ├── Second page: 20 reads
    └── Only load when user scrolls

TOTAL: ~100 reads per admin session (on-demand)
     = 1,000 reads/month (10 admins) 🟢💚
     
SAVINGS: 99.7% reduction (4.3M → 1k)
```

---

## ☁️ CLOUD FUNCTIONS DATA FLOW

### **sendBusArrivalNotifications Function**

```
┌─────────────────────────────────────────────────────────┐
│         Cloud Scheduler (Firebase Functions)            │
│              Triggers every 2 minutes                    │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Query: students.where('notified', '==', false)         │
│         .limit(100)                                     │
│  → Returns 100 student documents                        │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Group students by assignedBusId                        │
│  → Creates Map<busId, List<students>>                   │
│    Example: {                                           │
│      'bus1': [student1, student2, student3],            │
│      'bus2': [student4, student5],                      │
│    }                                                    │
└───────────────────────┬─────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  For each busId in Map:                                 │
│    1. Query bus_status/{busId}.get()                    │
│    2. Calculate ETA for each student's stop             │
│    3. If ETA ≤ notificationPreferenceByTime:            │
│       → Send FCM notification                           │
│       → Update student.notified = true                  │
└─────────────────────────────────────────────────────────┘

READS PER INVOCATION:
├── students query: 100 reads
├── bus_status queries: 10 reads (avg 10 unique buses)
└── TOTAL: 110 reads per run

INVOCATIONS PER DAY: 720 (every 2 minutes)
READS PER DAY: 79,200
READS PER MONTH: 2.4M 🔥💰

────────────────────────────────────────────────────────────

OPTIMIZED (every 5 minutes + limit 50):
├── students query: 50 reads
├── bus_status queries: 5 reads
└── TOTAL: 55 reads per run

INVOCATIONS PER DAY: 288 (every 5 minutes)
READS PER DAY: 15,840
READS PER MONTH: 475k 🟢💚

SAVINGS: 80% reduction (2.4M → 475k)
```

---

## 🔄 BUS LOCATION UPDATE FLOW (Driver App)

```
┌────────────────────────────────────────────────────────┐
│              DRIVER MOBILE APP                         │
│        (Background Location Service)                   │
└─────────────────────┬──────────────────────────────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │  GPS Location Update   │
         │  (Every 5-10 seconds)  │
         └────────────┬───────────┘
                      │
                      ▼
┌────────────────────────────────────────────────────────┐
│  DriverController.updateBusLocation()                  │
│  1. Get current GPS coordinates                        │
│  2. Calculate current speed                            │
│  3. Update bus_status document                         │
│  4. Calculate remaining stops                          │
│  5. Update ETA for each stop                           │
└────────────────────┬───────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────────┐
│  Firestore Write: bus_status/{busId}                   │
│  {                                                     │
│    currentLocation: { lat, lng },                      │
│    currentSpeed: 45.2,                                 │
│    currentStatus: 'Active',                            │
│    remainingStops: [...],                              │
│    lastUpdated: timestamp,                             │
│    routePolyline: [...]                                │
│  }                                                     │
└────────────────────┬───────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────────┐
│         STUDENT APPS RECEIVE UPDATE                    │
│  (via snapshots() listener or polling)                 │
│  → Map updates bus marker position                     │
│  → Remaining stops recalculated                        │
│  → ETA displayed to students                           │
└────────────────────────────────────────────────────────┘

WRITES PER DAY:
├── 1 bus × 6 updates/min × 60 min/hour × 8 hours = 2,880 writes/day
├── 20 buses × 2,880 = 57,600 writes/day
└── MONTHLY: 1.7M writes/month

READS PER DAY (if 100 students using snapshots()):
├── 100 students × 6 updates/min × 60 min × 0.5 hours = 180,000 reads
└── MONTHLY: 5.4M reads/month 🔥💰

────────────────────────────────────────────────────────────

OPTIMIZED (polling every 30 seconds):
├── 100 students × 2 updates/min × 60 min × 0.5 hours = 6,000 reads
└── MONTHLY: 180k reads/month 🟢💚

SAVINGS: 97% reduction (5.4M → 180k)
```

---

## 📊 DATA STORAGE BREAKDOWN

### **Firestore Document Sizes**

```
students/{id}          → ~1 KB per document
  ├── 500 students     = 500 KB
  └── COST: Storage free up to 1GB

drivers/{id}           → ~0.5 KB per document
  ├── 50 drivers       = 25 KB
  
schools/{id}           → ~2 KB per document
  ├── 10 schools       = 20 KB
  
schools/{id}/buses/    → ~5 KB per document (includes route polyline)
  ├── 50 buses         = 250 KB
  
bus_status/{id}        → ~10 KB per document (large polyline data)
  ├── 50 buses         = 500 KB
  ├── Updated 6x/min   = 3,000 KB/hour writes
  └── COST: 1.7M writes/month × $0.18/100k = $30/month

notifications/{id}     → ~2 KB per document
  ├── 10,000 messages  = 20 MB
  └── ⚠️ Never deleted = Growing indefinitely
  
TOTAL STORAGE: ~25 MB (well within free tier)
PROBLEM: Write frequency on bus_status is expensive
```

---

## 🎯 OPTIMIZATION STRATEGY SUMMARY

### **Before Optimization:**
```
Mobile App:     5.4M reads/month  ($32/month)
Web App:        4.3M reads/month  ($26/month)
Cloud Functions: 2.4M reads/month ($14/month)
Bus Updates:    1.7M writes/month ($30/month)
────────────────────────────────────────────
TOTAL:          13.8M operations  ($102/month)
```

### **After Optimization:**
```
Mobile App:       180k reads/month  ($0/month - free tier)
Web App:            1k reads/month  ($0/month - free tier)
Cloud Functions:  475k reads/month  ($0/month - free tier)
Bus Updates:     1.7M writes/month  ($20/month - optimized)
────────────────────────────────────────────────────────────
TOTAL:           2.4M operations    ($20/month)

SAVINGS: $82/month (80% reduction)
```

---

## 🔐 SECURITY LAYER (To Be Implemented)

```
┌────────────────────────────────────────────────────────┐
│            Firebase Authentication Layer               │
│  User logs in → Gets ID Token with Custom Claims      │
└──────────────────────┬─────────────────────────────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │   Custom Claims:     │
            │   {                  │
            │     role: 'student'  │
            │     schoolId: '...'  │
            │   }                  │
            └──────────┬───────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────┐
│              Firestore Security Rules                  │
│                                                        │
│  match /students/{studentId} {                         │
│    allow read: if request.auth.uid == studentId        │
│                || request.auth.token.role == 'admin'; │
│    allow write: if request.auth.token.role == 'admin';│
│  }                                                     │
│                                                        │
│  match /bus_status/{busId} {                           │
│    allow read: if request.auth != null;                │
│    allow write: if request.auth.token.role == 'driver'│
│                 || request.auth.token.role == 'admin'; │
│  }                                                     │
└────────────────────────────────────────────────────────┘
```

---

**See full optimization guide:** `FIREBASE_ANALYSIS_AND_OPTIMIZATION.md`
**Quick implementation:** `QUICK_OPTIMIZATION_CHECKLIST.md`
